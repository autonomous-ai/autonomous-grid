# Drafts

Forward-looking specs and proposals for expanding Grid — **not yet shipped**, and not part of
the current product docs. Subject to change.

- [protocol.md](protocol.md) — AI MapReduce: `map`/`reduce`/`split` ops that turn Grid from a
  router into a parallel agent engine.
