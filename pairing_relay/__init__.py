"""A minimal Grid pairing relay: splices a phone's socket to its desktop's.

See README.md. Nothing here is imported by the `grid` CLI, and
`[tool.setuptools.packages.find]` in pyproject.toml does not include it, so it
never reaches the published wheel.
"""
